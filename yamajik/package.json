{
  "name": "mal",
  "private": true,
  "version": "1.0.0",
  "description": "Make a Lisp (mal) language implemented in TypeScript",
  "scripts": {
    "build": "tsfmt -r && tsc -p ./",
    "test:step0": "npm run build && node dist/step0_repl.js",
    "test:step1": "npm run build && node dist/step1_read_print.js",
    "test:step2": "npm run build && node dist/step2_eval.js",
    "test:step3": "npm run build && node dist/step3_environment.js",
    "test:step4": "npm run build && node dist/step4_if_fn_do.js",
    "test:step5": "npm run build && node dist/step5_tco.js",
    "test:step6": "npm run build && node dist/step6_file.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^9.6.5",
    "typescript": "^2.8.1",
    "typescript-formatter": "^7.2.0"
  }
}
